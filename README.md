# atexteffects

A curated catalog of 110 animated text effects with agnostic prompts to reproduce them in any stack or AI tool.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `pnpm install`
2. Run the app:
   `pnpm dev`

